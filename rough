try {
    boolean importAdded = false;

    while ((line = reader.readLine()) != null) {
        // Replace "DBRecordSet" with "ResultSet"
        if (line.contains("DBRecordSet")) {
            line = line.replace("DBRecordSet", "ResultSet");

            // Check if the import statement is already added
            if (!importAdded && line.trim().startsWith("package ")) {
                // Add the import statement just after the package declaration
                result += "import java.sql.ResultSet;\n";
                importAdded = true;
            }
        }

        // Additional logic for other replacements or conversions can go here
        result += line + "\n";
    }
} catch (Exception e) {
    e.printStackTrace();
}











final String cstrCurrentProc = "Form_Load"
;    Const(cintNoRowsInTable As Integer == 0);
    final String cstrOutputFile = "C:\\WrapperTest;    DBRecordSet rst = null;
    DBField fld = null;
    String strProperty1 = "";
    String strProperty2 = "";
    String strProperty3 = "";
    Date dteToday = null;
    Scripting.FileSystemObject fso = null;


 Const cstrCurrentProc       As String = "Form_Load"
    Const cstrOutputFile        As String = "C:\WrapperTest.txt"
    Dim rst                     As ADODB.Recordset
    Dim fld                     As ADODB.Field
        Dim dteToday                As Date
    Dim fso                     As Scripting.FileSystemObject
