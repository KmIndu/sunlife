line = line.replace("for (intAllIndex = LBound(astrAllEnvironments); intAllIndex <= astrAllEnvironments.length; intAllIndex++) {", "")
                           .replace("//SQL_INTEGRATED_SECURITY If fnIsAuthorized(strUserID, astrAllEnvironments(intAllIndex)) Then", "")
                           .replace("astrAuthorizedEnvironments[intAuthIndex] = astrAllEnvironments[intAllIndex];", "")
                           .replace("intAuthIndex = intAuthIndex + 1;", "")
                           .replace("//SQL_INTEGRATED_SECURITY End If", "");








for (intAllIndex = LBound(astrAllEnvironments); intAllIndex <= astrAllEnvironments.length; intAllIndex++) {
                      //SQL_INTEGRATED_SECURITY If fnIsAuthorized(strUserID, astrAllEnvironments(intAllIndex)) Then
                      astrAuthorizedEnvironments[intAuthIndex] = astrAllEnvironments[intAllIndex];
                      intAuthIndex = intAuthIndex + 1;
                      //SQL_INTEGRATED_SECURITY End If
                  }
